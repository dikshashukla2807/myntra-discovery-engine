This app is the PM dashboard for the Myntra Discovery Engine.

From the repository root:

```bash
cd frontend && npm install && npm run dev
```

The UI expects the FastAPI backend on `http://127.0.0.1:43124` (rewritten via `/api/*`).

See the root README for the full stack.
